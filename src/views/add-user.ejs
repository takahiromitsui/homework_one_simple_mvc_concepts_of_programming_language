<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Create a User</title>
		<style>
			main {
				display: flex;
				flex-direction: column;
				align-items: center;
			}
			.form-control input{
				display: block;
				font: inherit;
				font-size: 1.5rem;
				border: 1px solid #dbc441;
				border-radius: 2px;
				margin: 1rem 0;
			}
			button {
				font:inherit;
				font-size: 1.5rem;
				border: none;
				border-radius: 2px;
				background-color: #dbc441;
				color: #fff;
				cursor: pointer;
			}
			button:hover {
				background-color: #f0e68c;
				color: #000;
			}
			button:active {
				background-color: #dbc441;
				color: #fff;
			}
			button:disabled,
			button[disabled] {
				border: 1px solid #999999;
				background-color: #cccccc;
				color: #666666;
			}
		</style>
		<script>
			function turnOnButton() {
				const name = document.getElementById('user-name-input').value;
				if (name.trim().length !== 0) {
					return document.getElementById('submit-btn').disabled = false;
				}
				return document.getElementById('submit-btn').disabled = true;
			}
		</script>
	</head>
	<body>
		<main>
			<h1>Hiya, please type in your name</h1>
			<form action="/user" method="POST">
				<div class="form-control">
					<input type="text" name="username" placeholder="Username" id="user-name-input"
					onkeyup="turnOnButton()" />
				</div>
				<button disabled id="submit-btn" type="submit">Create a user</button>
			</form>
		</main>
  </body>
</html>
